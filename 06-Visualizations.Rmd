# Discussion {#discussion}

This is where we will write things, ahaha

## Other fun things - Interactive Chloropleths

```{r}
library(plotly)
library(tidyverse)
library(purrr)
library(RColorBrewer)
library(tmap)
```

```{r}
g <- list(
  scope = 'usa',
  projection = list(type = 'albers usa'),
  lakecolor = toRGB('white')
)
```

```{r}
avgDM <- distinct(debtMedian)

plot_geo() %>%
  add_trace(
    z = ~avgDM$avgDebt,
    span = I(0),
    colorscale = 'viridis',
    locations = avgDM$state, 
    locationmode = 'USA-states'
  ) %>%
  colorbar(title = NULL) %>%
  layout(geo = g, title = "Avg. Faculty Salary by State (in USD)")
```

